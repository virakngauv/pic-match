# Browser-visible game server endpoint. Leave unset for local development,
# where the app connects to the page host (localhost, LAN IP, *.local) on
# port 3200.
NEXT_PUBLIC_GAME_SERVER_URL=

# Game server runtime
PORT=3200
# Bind 0.0.0.0 to test from other devices on your LAN.
HOST=127.0.0.1
# Explicit production allowlist. Outside production, private-network origins
# (localhost, 127.0.0.1, RFC1918 ranges, *.local) are always accepted too.
# Override with ALLOW_PRIVATE_NETWORK_ORIGINS=true|false.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
LOG_LEVEL=info

# Clerk authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# PostHog analytics
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
